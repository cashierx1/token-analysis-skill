# Token Analysis Skill — API Keys
# Copy this file to .env and fill in your keys.
# All keys are optional — the agent falls back to web search if unavailable.

# Twitter data provider — which API to use for X/Twitter research
# Options: socialdata | xapi
# If not set, uses whichever key is available (prefers socialdata if both set)
TWITTER_PROVIDER=

# SocialData API — alternative Twitter data provider (pay-per-use, $0.0002/tweet)
# Get it: https://socialdata.tools → sign up → dashboard → API key
SOCIALDATA_API_KEY=

# X/Twitter API v2 — social discourse and dev activity
# Get it: https://developer.x.com/en/portal/dashboard → create app → Bearer Token
X_BEARER_TOKEN=

# Farcaster / Neynar API — crypto-native social data + wallet verification
# Get it: https://dev.neynar.com/ → create app → API key
NEYNAR_API_KEY=
#
# Neynar plan — controls which endpoints the agent will use:
#
#   beginner  (free, 200K credits)
#             ✓ user profiles (by_username, bulk by FID)
#             ✓ wallet verification (verified_addresses)
#             ✓ reverse lookup (user by wallet address)
#             ✗ cast search → fallback: web search, warpcast.com, Pinata Hub
#             ✗ channels / feeds → fallback: warpcast.com/~/channel/NAME
#
#   starter   ($9/mo, 1M credits)
#             ✓ everything in beginner
#             ✓ cast search by keyword / by channel
#             ✓ channel info (followers, members)
#             ✓ channel feeds
#
NEYNAR_PLAN=beginner

# Telegraph — deep report publishing
# One-time setup (run this, then paste the access_token below):
#   curl -s "https://api.telegra.ph/createAccount?short_name=TokenAnalysis&author_name=Fair&author_url=https://faircaster.xyz"
TELEGRAPH_TOKEN=

# Output language — analysis reports and Telegraph articles will use this language.
# Leave empty or set to "en" for English (default).
# Examples: en, ru, zh, ja, ko, es, fr, de, pt, ar
OUTPUT_LANG=

# Google Sheets — analysis log
# Setup: open your Google Sheet → Extensions → Apps Script → paste code from SKILL.md → Deploy as Web App
# Paste the web app URL below.
GOOGLE_SHEETS_WEBHOOK=
